# Custom swagger-ui docker image

This is just to replace the index.html from the default index.html in the swagger-ui docker image.

This adds option `docExpansion: 'none'` to the swagger-ui options.
